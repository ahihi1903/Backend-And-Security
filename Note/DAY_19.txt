DAY 19 — Pagination + Search + Filter + Sort
API chuyên nghiệp = không trả tất cả dữ liệu 1 lần

1️⃣ Vấn đề hiện tại
Bạn đang:

GET /users
→ trả toàn bộ users

❌ Sai khi dữ liệu lớn:

1000 users → lag
100k users → crash

2️⃣ Giải pháp chuẩn
GET /users?page=1&limit=5
GET /users?search=an
GET /users?sort=name
GET /users?role=admin

3️⃣ Query params là gì?
?key=value&key=value
Ví dụ:
/users?page=1&limit=2

4️⃣ Code controller chuẩn
📁 userController.js

import { getUsers } from "../model/users.js";

export function getAllUsers(req, res) {
  const users = getUsers();

  // lấy query
  const page = Number(req.query.page) || 1;
  const limit = Number(req.query.limit) || 5;
  const search = req.query.search || "";
  const sort = req.query.sort || "";

  // SEARCH
  let result = users.filter(u =>
    u.name.toLowerCase().includes(search.toLowerCase())
  );

  // SORT
  if (sort === "name") {
    result.sort((a, b) => a.name.localeCompare(b.name));
  }

  // PAGINATION
  const start = (page - 1) * limit;
  const end = start + limit;

  const paginated = result.slice(start, end);

  // RESPONSE chuẩn
  res.end(JSON.stringify({
    total: result.length,
    page,
    limit,
    data: paginated
  }));
}
5️⃣ Kết quả test Postman
🔹 1. Pagination
GET /users?page=1&limit=2
{
  "total": 5,
  "page": 1,
  "limit": 2,
  "data": [
    { "id": 1, "name": "An" },
    { "id": 2, "name": "Binh" }
  ]
}
🔹 2. Search
GET /users?search=an
{
  "data": [
    { "id": 1, "name": "An" }
  ]
}
🔹 3. Sort
GET /users?sort=name
[
  { "name": "An" },
  { "name": "Binh" }
]
🔹 4. Kết hợp
GET /users?page=1&limit=2&search=a&sort=name
6️⃣ Middleware parse query (nâng cấp)

Trong server.js bạn nên có:

const parsedUrl = new URL(req.url, `http://${req.headers.host}`);
req.query = Object.fromEntries(parsedUrl.searchParams);
7️⃣ Chuẩn API response công ty
{
  "success": true,
  "total": 10,
  "page": 1,
  "limit": 5,
  "data": [...]
}
8️⃣ Sai lầm phổ biến
❌ không parse Number(page)
❌ limit quá lớn (DDOS)
❌ không validate query
❌ trả toàn bộ data
9️⃣ Bonus nâng cấp
Giới hạn limit:
const limit = Math.min(Number(req.query.limit) || 5, 20);
🔟 Flow thực tế công ty
request
 ↓
auth
 ↓
role
 ↓
controller (filter + paginate)
 ↓
response chuẩn
<----------------------->
🧠 1. Tư duy cốt lõi (quan trọng nhất)

Bạn hãy tưởng tượng:

👉 Bạn có 1 danh sách users (giống Excel)

An
Binh
Cuong
Dung
Hanh
❓ Người dùng yêu cầu:
"Cho tôi:
- chỉ những người có chữ 'a'
- sắp xếp theo tên
- lấy 2 người đầu tiên"
👉 Bạn sẽ làm gì bằng tay?
B1: Lọc (SEARCH)
An
Hanh
B2: Sắp xếp (SORT)
An
Hanh
B3: Lấy 2 thằng đầu (PAGINATION)
An
Hanh
🔥 👉 Đây chính là tư duy backend
data gốc
 ↓
lọc (filter)
 ↓
sắp xếp (sort)
 ↓
cắt (paginate)
❗ QUAN TRỌNG

Bạn phải nhớ:

KHÔNG BAO GIỜ:
paginate → rồi filter ❌

LUÔN:
filter → sort → paginate ✅