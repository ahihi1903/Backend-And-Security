🔥 DAY 22 — Validation chuẩn công ty (Joi/Zod)

Đây là bước cực kỳ quan trọng vì hiện tại validation của bạn đang kiểu:

if (!username) {
  throw createError(400, "Username required");
}

if (username.length < 3) {
  throw createError(400, "Too short");
}

❌ Vấn đề:

code dài
khó maintain
controller bẩn
repeated code
🎯 Công ty làm kiểu gì?

Dùng:

Joi
hoặc
Zod

Mình khuyên bạn học Zod trước vì:

✔ hiện đại hơn
✔ dễ đọc
✔ TypeScript cực mạnh
✔ công ty mới dùng nhiều
🚀 Mục tiêu Day 22

Thay từ:

if (!password)
if (password.length < 6)
if (typeof username !== "string")

→ thành:

registerSchema.parse(req.body)
STEP 1 — Cài package
npm install zod
STEP 2 — Tạo folder validation
src/
├── validations/
│   └── authValidation.js
STEP 3 — Register schema

📁 validations/authValidation.js

import { z } from "zod";

export const registerSchema = z.object({
  username: z
    .string()
    .min(3, "Username too short")
    .max(20, "Username too long"),

  password: z
    .string()
    .min(6, "Password too short"),

  role: z
    .enum(["admin", "user"])
    .default("user")
});
🧠 Giải thích
.string()

Phải là string.

.min(3)

Ít nhất 3 ký tự.

.enum()

Chỉ cho:

admin
user
STEP 4 — Login schema
export const loginSchema = z.object({
  username: z.string(),
  password: z.string()
});
STEP 5 — Validation middleware

📁 middlewares/validate.js

export default function validate(schema) {
  return (req, res, next) => {
    try {
      req.body = schema.parse(req.body);

      next();
    } catch (error) {
      return res.status(400).json({
        message: error.errors[0].message
      });
    }
  };
}
STEP 6 — Route dùng middleware

📁 routes/authRoutes.js

import validate from "../middlewares/validate.js";

import {
  registerSchema,
  loginSchema
} from "../validations/authValidation.js";

router.post(
  "/register",
  validate(registerSchema),
  register
);

router.post(
  "/login",
  validate(loginSchema),
  login
);
STEP 7 — Controller sạch hơn cực nhiều
Trước
if (!username)
if (!password)
if (password.length < 6)
Sau
export async function register(req, res) {
  const { username, password, role }
    = req.body;

  await registerService(
    username,
    password,
    role
  );

  res.status(201).json({
    message: "User created"
  });
}

🔥 Controller giờ rất sạch.

🧪 Test Postman
❌ Username ngắn

Request:

{
  "username":"ab",
  "password":"123456"
}

Response:

{
  "message":"Username too short"
}
❌ Password ngắn
{
  "message":"Password too short"
}
❌ Role sai
{
  "username":"admin",
  "password":"123456",
  "role":"superadmin"
}

Response:

{
  "message":"Invalid enum value"
}
✅ Đúng
{
  "username":"admin",
  "password":"123456",
  "role":"admin"
}

Response:

{
  "message":"User created"
}
🎯 Vì sao Day 22 quan trọng?

Đi thực tập bạn sẽ thấy:

controller cực sạch
validation tách riêng

Chứ không ai viết:

if (!username)
if (!password)
if (password.length < 6)

khắp project.

📁 Structure chuẩn sau Day22
project/
│
├── controllers/
├── services/
├── models/
├── routes/
├── middlewares/
├── validations/ 🔥
├── utils/
🚀 Level hiện tại
Level 9.3

Bạn đang bắt đầu viết backend giống công ty thật.